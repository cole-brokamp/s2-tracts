[package]
name = "s2-tracts"
version = "0.1.0"
edition = "2024"
description = "Offline S2 lookup of fixed 2010 and 2020 decennial Census tracts"
license = "MIT"
repository = "https://github.com/cole-brokamp/s2-tracts"
exclude = ["/work", "/dist"]

[[bin]]
name = "s2-tracts"
path = "src/main.rs"

[dependencies]
s2 = { version = "0.2.0", default-features = false }
flatgeobuf = { version = "6.0.1", default-features = false }
geo = { version = "0.32", default-features = false }
thiserror = "2"
serde_json = "1"

[dev-dependencies]
geozero = { version = "0.15.1", default-features = false, features = ["with-geo"] }
tempfile = "3"
